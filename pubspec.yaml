name: gerador_recibo
description: App para geração de recibos de serviços prestados
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  pdf: ^3.10.7
  printing: ^5.12.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  crypto: ^3.0.3
  flutter_secure_storage: ^9.0.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

